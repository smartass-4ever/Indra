# Indra — requirements

# Core
numpy>=1.24.0
requests>=2.31.0               # Bright Data API calls
cryptography>=41.0.0           # Fernet AES-128 encryption — production-grade
anthropic>=0.20.0              # LLM analysis — auto-activated via ANTHROPIC_API_KEY
sentence-transformers>=2.2.0   # semantic embeddings for Mnemon cache layer

# Optional
# openai>=1.0.0                # alternative LLM — pip install mnemon-ai[openai]
# redis>=5.0.0                 # distributed scale — pip install mnemon-ai[redis]

# Dev
# pytest>=7.0.0
# pytest-asyncio>=0.21.0
# black>=23.0.0
# mypy>=1.0.0
